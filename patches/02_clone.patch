--- /mnt/repos/repos/github/nocturn_tools/uClibc/uClibc-0.9.33.2_orig/libc/sysdeps/linux/arm/clone.S
+++ /mnt/repos/repos/github/nocturn_tools/uClibc/uClibc-0.9.33.2/libc/sysdeps/linux/arm/clone.S
@@ -104,26 +104,24 @@
 	@ get flags
 	mov	r0, r2
 	@ new sp is already in r1
-	@ load remaining arguments off the stack
-	stmfd	sp!, {r4}
-	ldr	r2, [sp, #4]
-	ldr	r3, [sp, #8]
-	ldr	r4, [sp, #12]
 	DO_CALL (clone)
 	movs	a1, a1
 	blt	__error
-	ldmnefd	sp!, {r4}
 	IT(t, ne)
 #if defined(__USE_BX__)
 	bxne	lr
 #else
 	movne	pc, lr
 #endif
-
 	@ pick the function arg and call address off the stack and execute
 	ldr	r0, [sp, #4]
-	mov	lr, pc
-	ldr 	pc, [sp]
+#if defined(__USE_BX__)
+	ldr 	r1, [sp]
+	blx	r1
+#else	/* Old code - a bug? */
+	mov     lr, pc
+	ldr     pc, [sp]
+#endif
 
 	@ and we are done, passing the return value through r0
 	b	HIDDEN_JUMPTARGET(_exit)
